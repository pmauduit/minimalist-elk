input {
  tcp {
    port => 9000
    codec => json
  }
}

filter {
  date {
    match => [ "time", "ISO8601" ]
  }
}

output {
  elasticsearch { hosts => ["elastic:9200"] }
  stdout { codec => rubydebug }
}
